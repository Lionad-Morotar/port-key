<!-- Auto Generated - Do Not Edit -->
# PortKey

<p align="center">
  <img width="200" src="https://raw.githubusercontent.com/Lionad-Morotar/port-key/main/public/logo.png" />
</p>

<p align="center">
  <strong>PortKey：استراتيجية بسيطة وعملية لتسمية المنافذ</strong>
</p>

<p align="center">
  <!-- LANGUAGES=("cn" "es" "fr" "de" "ja" "ko" "ru" "ar" "pt" "it") -->
  <a href="./docs/README.cn.md">中文</a> | <a href="./docs/README.es.md">Español</a> | <a href="./docs/README.fr.md">Français</a> | <a href="./docs/README.de.md">Deutsch</a> | <a href="./docs/README.ja.md">日本語</a> | <a href="./docs/README.ko.md">한국어</a> | <a href="./docs/README.ru.md">Русский</a> | <a href="./docs/README.ar.md">العربية</a> | <a href="./docs/README.pt.md">Português</a> | <a href="./docs/README.it.md">Italiano</a>
</p>

## نبذة

توليد المنافذ باستخدام تعيين الحروف إلى الأرقام على لوحة المفاتيح

عندما تشغّل عدداً كبيراً من المشاريع محلياً، يصبح اختيار أرقام المنافذ أمراً مزعجاً.

- خلال السنوات القليلة الماضية، ظهرت *الكثير* من المشاريع الجديدة. لتجربتها فعلياً، تحتاج غالباً إلى تشغيلها محلياً — وعندها تبدأ المنافذ في التعارض.
- إذا كنت تريد الحفاظ على علامات تبويب المتصفح (أو الإشارات المرجعية) ثابتة، فلا ينبغي أن يتغير منفذ المشروع باستمرار.

على سبيل المثال، لديّ أكثر من عشرة تطبيقات Nuxt على جهازي. إذا كانت جميعها تستخدم المنفذ `3000` افتراضياً، فهذا لن يعمل بوضوح. لذا ابتكرت قاعدة بسيطة ومتسقة لتسمية المنافذ من أجل "تعيين" منفذ لكل مشروع.

[منشور المدونة المصدر](https://lionad.art/articles/simple-naming-method)

### الفكرة الأساسية

بدلاً من اختيار أرقام عشوائية، عيّن **اسم المشروع إلى أرقام بناءً على لوحة المفاتيح**، بحيث يكون المنفذ *قابلاً للقراءة* و*سهل التذكر*.

طالما أن النتيجة تقع ضمن نطاق المنافذ الصالح (**1024–65535**) ولا تصطدم بالمنافذ المحجوزة/النظامية، يمكنك استخدامها مباشرةً.

بشكل أكثر تحديداً: باستخدام لوحة مفاتيح QWERTY قياسية، عيّن كل حرف إلى رقم واحد بناءً على **موضعه في الصف/العمود**.

مثال:

`"cfetch"` → `c(3) f(4) e(3) t(5) c(3) h(6)` → `34353`（رقم المنفذ）

ثم يمكنك أخذ أول 4 أرقام (مثل `3453`)، أو الاحتفاظ بمزيد من الأرقام (مثل `34353`). كلاهما مقبول.

إذا كان المشروع يحتاج إلى منافذ متعددة (واجهة أمامية، خادم خلفي، قاعدة بيانات، إلخ)، اختر **أحد** النهجين التاليين:

1. استخدم بادئة المشروع، ثم ألحق "لاحقة الدور"
   - بالنسبة لـ `"cfetch"`، خذ `3435` كأساس
   - الواجهة الأمامية (`fe`، أي `43`) → `34354`
   - الخادم الخلفي (`server`) → `34352`
   - قاعدة البيانات (`mongo`) → `34357`
   - ...وهكذا

2. استخدم بادئة المشروع، ثم عيّن أدواراً متتالية
   - بالنسبة لـ `"cfetch"`، خذ `3435` كأساس
   - الويب → `34351`
   - الخادم الخلفي → `34352`
   - قاعدة البيانات → `34353`
   - ...وهكذا

### نطاق المنافذ الصالح

- يجب أن تكون المنافذ ضمن **1024–65535** (المنافذ النظامية 0–1023 محظورة).
- **المنافذ النظامية (0–1023)**: مخصصة من IETF. محظورة تماماً.
- **منافذ المستخدم (1024–49151)**: مخصصة من IANA. استخدمها بحذر لأنها قد تتعارض مع الخدمات المسجلة.
- **المنافذ الديناميكية/الخاصة (49152–65535)**: غير مخصصة. الأكثر أماناً للاستخدام الخاص أو الديناميكي.

---

## كيفية الاستخدام

الأمر البسيط:

```sh
npx -y @lionad/port-key <your-project-name>
```

أو إذا أردت خادم MCP عبر stdio:

```sh
npx -y @lionad/port-key-mcp
```

```json
{
  "mcpServers": {
    "port-key": {
      "command": "npx",
      "args": ["@lionad/port-key-mcp"]
    }
  }
}
```


### خيارات CLI

- `-m, --map <object>`: تعيين مخصص (كائن JSON أو حرفي يشبه JS)
- `--lang <code>`: لغة الإخراج (حالياً `en` و `cn` فقط، الافتراضي: `cn`)
- `-d, --digits <count>`: عدد الأرقام المفضل للمنفذ (4 أو 5، الافتراضي: 4)
- `--padding-zero <true|false>`: املأ بالأصفار اللاحقة إلى عدد الأرقام المفضل عندما يكون الإدخال قصيراً (الافتراضي: true). مثال: `"air"` -> `1840`, `"1234" --digits 5` -> `12340`
- `-h, --help`: عرض المساعدة

أمثلة:

```bash
npx @lionad/port-key cfetch # -> 3435
npx @lionad/port-key cfetch --digits 4  # -> 3435 (منفذ من 4 أرقام)
npx @lionad/port-key cfetch --digits 5  # -> 34353 (منفذ من 5 أرقام)
```

ملاحظات:
- لغة السجل الافتراضية هي `cn`. استخدم `--lang en` لعرض الرسائل بالإنجليزية.
- استخدم `-h` أو `--help` لعرض المساعدة.

### التكوين

يقرأ PortKey تكوين المستخدم الاختياري من:

- `~/.port-key/config.json`

مثال كامل:

```json
{
  // عدد الأرقام المفضل للمنفذ (4 أو 5)
  "preferDigitCount": 5,
  // املأ بالأصفار اللاحقة إلى عدد الأرقام المفضل عندما يكون الإدخال قصيراً (الافتراضي: true)
  "paddingZero": true,
  // تعيين مخصص من الحروف إلى الأرقام
  "blockedPorts": [3000, 3001, 3002, 6666],
  // حدود نطاق المنافذ (شاملة)
  "minPort": 1024,
  "maxPort": 49151
}
```

---

## للمطور

### هيكل المشروع

- يستخدم هذا المستودع pnpm monorepo؛ الحزمة الأساسية موجودة في `packages/core`.
- التثبيت: نفّذ `pnpm install` في الجذر.
- تشغيل الاختبارات: `pnpm -C packages/core test` أو `pnpm -C packages/core test:watch`.
